import React from 'react'; // used when dealing with HTML
import ReactDOM from 'react-dom'; // controls the DOM of the document
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
